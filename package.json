{
  "name": "pdf-tools-website",
  "version": "2.0.0",
  "description": "Complete PDF Tools Website",
  "scripts": {
    "start": "cd backend && npm start",
    "install-backend": "cd backend && npm install",
    "build": "npm run install-backend"
  },
  "engines": {
    "node": "18.x"
  }
}
